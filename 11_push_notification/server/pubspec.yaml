name: server
description: "Dart CLI to send FCM topic notifications for sample 11."
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.12.2

dependencies:
  googleapis_auth: ^2.0.0
  http: ^1.6.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
